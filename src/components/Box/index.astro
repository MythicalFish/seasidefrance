---
import clsx from 'clsx';
const { class: className, cozy = false } = Astro.props;
---

<div
  class={clsx(
    'bg-white p-8 rounded-lg shadow-lg overflow-hidden hover:shadow-xl transition-shadow duration-300 mb-4',
    className,
    cozy && 'md:p-12 lg:p-16'
  )}
>
  <slot />
</div>
