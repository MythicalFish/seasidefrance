---
import clsx from 'clsx';
const { id, class: className, cozy = false } = Astro.props;
import styles from './styles.module.css';
---

<section id={id} class={clsx(styles.hero, className)}>
  <slot />
</section>
