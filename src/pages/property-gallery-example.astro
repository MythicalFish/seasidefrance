---
import Layout from '../layouts/Layout.astro';
import PropertyGallery from '../components/PropertyGallery.astro';

// Example property ID - use an actual ID that exists in your src/photos directory
const propertyId = 280274;
---

<Layout title="Property Gallery Example">
  <main>
    <h1>Property Gallery Example</h1>

    <section>
      <PropertyGallery propertyId={propertyId} title={`Images for Property ${propertyId}`} />
    </section>

    <div class="documentation">
      <h2>How to Use the Gallery</h2>
      <p>
        This gallery component uses Astro's built-in image optimization features with dynamic
        imports using <code>import.meta.glob</code>. All images in the <code
          >src/photos/{propertyId}/</code
        >
        directory will be automatically loaded and optimized.
      </p>

      <h3>Usage</h3>
      <pre><code>
import PropertyGallery from '../components/PropertyGallery.astro';

&lt;PropertyGallery 
  propertyId={280274}
  title="Optional Gallery Title"
/&gt;
      </code></pre>

      <h3>References</h3>
      <ul>
        <li>
          <a href="https://cosmicthemes.com/blog/astro-dynamic-image-imports/" target="_blank">
            Dynamically Import Images in Astro
          </a>
        </li>
        <li>
          <a href="https://docs.astro.build/en/guides/images/" target="_blank">
            Astro Image Documentation
          </a>
        </li>
      </ul>
    </div>
  </main>
</Layout>

<style>
  main {
    max-width: 1200px;
    margin: 0 auto;
    padding: 2rem;
  }

  h1 {
    font-size: 2.5rem;
    margin-bottom: 2rem;
  }

  .documentation {
    margin-top: 4rem;
    padding: 2rem;
    background-color: #f9f9f9;
    border-radius: 0.5rem;
  }

  pre {
    background-color: #333;
    color: #fff;
    padding: 1rem;
    border-radius: 0.5rem;
    overflow-x: auto;
  }

  code {
    font-family: monospace;
  }

  ul {
    list-style-type: disc;
    margin-left: 1.5rem;
  }

  a {
    color: #0077cc;
    text-decoration: none;
  }

  a:hover {
    text-decoration: underline;
  }
</style>

